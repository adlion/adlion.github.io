import { InputAutoResizerDirective } from './input-auto-resizer.directive';

describe('InputAutoResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAutoResizerDirective();
    expect(directive).toBeTruthy();
  });
});
